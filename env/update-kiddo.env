# Ruta absoluta al repositorio KiddoClimbs.
# Actualiza este valor para que apunte a la ubicación correcta en tu máquina local.
SOURCE_REPOSITORY=/Users/tu_usuario/repos/KiddoClimbs

# Comando utilizado para compilar la aplicación antes de iniciar el servicio.
# Asigna un valor vacío (por ejemplo, `TAILSCALE_BUILD_COMMAND=`) para omitir la compilación.
TAILSCALE_BUILD_COMMAND="npm run build"

# Comando utilizado para iniciar el servicio en segundo plano.
TAILSCALE_START_COMMAND="npm run start"

# Puerto base a utilizar para el servicio. Si está ocupado, el script buscará
# automáticamente el siguiente puerto disponible en un rango determinado.
# Deja el valor vacío (por ejemplo, `TAILSCALE_PORT=`) para desactivar este
# comportamiento y delegar por completo la elección del puerto al comando de inicio.
TAILSCALE_PORT=3000

# Cantidad máxima de puertos consecutivos a intentar a partir de TAILSCALE_PORT.
# Debe ser un número entero positivo. Valor por defecto: 10.
TAILSCALE_PORT_SEARCH_LIMIT=10
